v=rep(x=4,by=5)
v
v=rep(x=4,by=5)
v
v=numeric(length=5)
v
apply?
apply?
?apply
?apply
setwd("C:/Users/jwmag/OneDrive/Desktop/Intro_to_biocomputing/R_exercises/exercise_8/Biocomputing-Ex-8")
data=read.csv(file="iris.csv",header=TRUE,spe=",")
data=read.csv(file="iris.csv",header=TRUE)
head(data)
head(data)
echo "Hello"
echo "Hello"
getwd()
dim(data)
data[149:150,4:5]
data[,5]
data[ ,5]
#part b) get number of observations for each species included in the data set
#just need to focus on fifth column
#find unique words and display number of times it is used
head(data)
sum(data$Species)
unique(data)
unique(data$Species)
#count number of rows have that species name
sum(data$Species == 'setosa')
#count number of rows that have species name versicolor
sum(data$Species == 'versicolor')
#count number of rows that have species name virginica
sum(data$Species == 'virginica')
[data$sepal.width>3.5,]
data[$sepal.width>3.5,]
data[sepal.width>3.5,]
data[Sepal.Width>3.5,]
data['Sepal.Width'>3.5,]
data[,'Sepal.Width'>3.5]
data[,'Sepal.Width' > 3.5]
data[,(data$'Sepal.Width' > 3.5)]
data[(data$Sepal.Width > 3.5),]
data[(data$Species == 'setosa'),]
#part d) write the data for the species setosa to comma-delimited file named 'setosa.csv'
#data[(data$Species == 'setosa'),]
cat(data[(data$Species == 'setosa'),],file = "setosa.csv",sep =",")
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),data$Petal.Length]
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),Petal.Length]
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),'Petal.Length']
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),'data$Petal.Length']
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),data$Petal.Length]
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[(data$Petal.Length == 'virginica'),4]
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[data$Petal.Length == 'virginica']
head(data)
#part e) calculate the mean, min, and max of Petal.Length for observations from virginica
#first print all rows of virginica but only the column "Petal.Length"
data[data$Species == 'virginica',"Petal.Length"]
#now we can find the requested arithmetic components for those values
sum(data[data$Species == 'virginica',"Petal.Length"])
#now we can find the requested arithmetic components for those values
mean(data[data$Species == 'virginica',"Petal.Length"])
min(data[data$Species == 'virginica',"Petal.Length"])
max(data[data$Species == 'virginica',"Petal.Length"])
data=read.csv(file="iris.csv",header=TRUE)
print(data)
dim(data)
#format of head function in bash
#head option file
#for nrows as indicated by user we will print that many rows, we will designate 5 for this example
nrows = 5
#we will use the variable "data" to allow us to read rows from the file assigned to data
data=read.csv(file="iris.csv",header=TRUE)
data[1:nrows,]
print(data[1:nrows,])
print(data[1:nrows,])
#we will use the variable "data" to allow us to read rows from the file assigned to data
data=read.table(file="iris.csv",header=TRUE,sep=",")
#using the "print" function should allow us to print in the terminal
print(data[1:nrows,])
